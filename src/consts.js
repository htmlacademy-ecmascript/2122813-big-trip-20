const WAYPOINTS_COUNT = 3;

const FilterType = {
  EVERYTHING: 'everything',
  FUTURE: 'future',
  PRESENT: 'present',
  PAST: 'past',
};

const SortType = {
  TIME: 'time',
  PRICE: 'price',
  DAY: 'day',
};

export { WAYPOINTS_COUNT, FilterType, SortType };
